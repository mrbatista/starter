{
  "name": "loopback-starter",
  "description": "Loopback Minimal Starter",
  "version": "0.0.0",
  "keywords": [
    "loopback-application",
    "loopback"
  ],
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "github:loopbackio/starter",
  "engines": {
    "node": "14 || 16 || 18"
  },
  "scripts": {
    "build": "lb-tsc",
    "build:watch": "lb-tsc --watch",
    "lint": "npm run eslint && npm run prettier:check",
    "lint:fix": "npm run eslint:fix && npm run prettier:fix",
    "prettier:cli": "lb-prettier \"**/*.ts\" \"**/*.js\"",
    "prettier:check": "npm run prettier:cli -- -l",
    "prettier:fix": "npm run prettier:cli -- --write",
    "eslint": "lb-eslint --report-unused-disable-directives .",
    "eslint:fix": "npm run eslint -- --fix",
    "pretest": "npm run rebuild",
    "test": "lb-mocha --allow-console-logs \"dist/__tests__\"",
    "posttest": "npm run lint",
    "test:dev": "lb-mocha --allow-console-logs dist/__tests__/**/*.js && npm run posttest",
    "docker:build": "docker build -t loopback-starter .",
    "docker:run": "docker run -p 3000:3000 -d loopback-starter",
    "premigrate": "npm run build",
    "migrate": "node ./dist/migrate",
    "preopenapi-spec": "npm run build",
    "openapi-spec": "node ./dist/openapi-spec",
    "prestart": "npm run rebuild",
    "start": "node -r source-map-support/register .",
    "clean": "lb-clean dist *.tsbuildinfo .eslintcache",
    "rebuild": "npm run clean && npm run build"
  },
  "files": [
    "README.md",
    "dist",
    "src",
    "!*/__tests__"
  ],
  "dependencies": {
    "@loopback/boot": "^7.0.1",
    "@loopback/core": "^6.0.1",
    "@loopback/repository": "^7.0.1",
    "@loopback/rest": "^14.0.1",
    "@loopback/rest-explorer": "^7.0.1",
    "@loopback/service-proxy": "^7.0.1",
    "@sourceloop/audit-log": "^8.0.1",
    "tslib": "^2.6.2"
  },
  "devDependencies": {
    "@loopback/build": "^11.0.1",
    "@loopback/eslint-config": "^15.0.1",
    "@loopback/testlab": "^7.0.1",
    "@types/node": "^20.12.12",
    "eslint": "^8.57.0",
    "source-map-support": "^0.5.21",
    "typescript": "~5.4.5"
  }
}
